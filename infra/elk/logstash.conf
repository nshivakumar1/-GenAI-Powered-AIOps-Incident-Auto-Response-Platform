input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  # Add any filters here if needed
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "aiops-incidents-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
